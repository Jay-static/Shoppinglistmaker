#allow the user to enter quantity

#allow the user to select pre-built recipies

#allow the user to email the final list to themselves

#create a simple ui to allow user to select items without having to enter its list number

#expand shopping items to include more food as well as common household items
